# create virtual env
python -m venv venv

# initiate the newly created environment (in your project folder)
source /Users/.../venv/bin/activate
#python -c "import sys; print('\n'.join(sys.path))" # see path dirs

# install requirements
pip install -r /Users/.../requirements.txt
